import { Link } from "react-router-dom";

function Footer(props) {
  return (
    <nav className="foot">
        <div>Only you can prevent magnesium fires</div>
    </nav>
  );
}

export default Footer;